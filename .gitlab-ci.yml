stages:
  - build
  - push

variables:
  IMAGE_NAME: "rimishghimire/assignment_22177669d:step3"

before_script:
  # Log in to Docker Hub using hardcoded credentials
  - docker login -u rimishghimire -p monkeyDluffy1

build:
  stage: build
  script:
    - docker build -t $IMAGE_NAME .

push:
  stage: push
  script:
    - docker push $IMAGE_NAME
